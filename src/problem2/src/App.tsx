import CurrencySwapForm from "./CurrencySwap/Container.tsx"

export default function App() {
  return (
    <main className="min-h-screen w-full bg-gradient-to-br from-gray-100 to-white flex items-center justify-center">
      <CurrencySwapForm />
    </main>
  )
}
